# =============================================================
# 本仓库只追踪「自研代码与文档」。
# 第三方素材、可再生的中间产物、大体积文件一律不入库。
# =============================================================

# ---- 第三方素材：受版权保护，不得公开发布 ----
建筑规范PDF/*.pdf

# ---- 中间产物：体积大，且可由脚本随时重新生成 ----
建筑规范PDF/ocr_text/
建筑规范PDF/chunks/
建筑规范PDF/_pilot/
建筑规范PDF/_ocr_test/
建筑规范PDF/*.json
建筑规范PDF/*.csv
建筑规范PDF/*.log

# ---- 密钥与本地配置：绝不允许入库 ----
# ⚠️ .env 里会放通义/Dify 的 API Key，一旦提交就是泄露
.env
.env.*
!.env.example
config.json

# ---- 向量库与向量缓存：含规范全文向量，体积大且可由脚本重建 ----
# cache/ 里的 .npy 是几十分钟算出来的向量缓存，不入库（但**不要手动删它**）
输出文档/chroma_db/
输出文档/**/chroma_db/
输出文档/cache/
*.npy
*.sqlite3

# ---- 一次性下载物（安装包等）----
_dl/

# ---- 本机启动脚本：内含本机 Python 绝对路径，换机即失效 ----
/start.bat

# ---- 本机输出目录：产物不入库，但自研脚本与说明文档要留 ----
输出文档/*
!输出文档/*.md
!输出文档/scripts/
!输出文档/scripts/*.py
!输出文档/scripts/*.sh
!输出文档/scripts/webcheck/
!输出文档/scripts/webcheck/*.py
# 下划线前缀 = 一次性探查/诊断脚本与临时图（_probe*.py / _diag_*.py / _bench.png）。
# 这类文件是过程产物，只对本机那次排查有意义，入库只会污染历史。
输出文档/scripts/_*
!输出文档/docs/
!输出文档/docs/*.md
# 检索评测是一整套自研工具（86 条评测集 + 跑分器 + 说明），要入库。
# 注意：必须先把**目录本身**放行，git 才会进去遍历，否则里面全被 `输出文档/*` 挡掉。
!输出文档/检索评测/
输出文档/检索评测/config.json
输出文档/检索评测/结果_*.json

# ---- WorkBuddy 工作区数据（会话记忆等，非项目代码）----
.workbuddy/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---- 编辑器 / 操作系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
*~
